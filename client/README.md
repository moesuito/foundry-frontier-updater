# Tauri Client

Codigo-fonte do executavel `foundry_frontier_sync.exe`.

```powershell
npm install
npm run tauri build
```

O binario release fica em:

```text
src-tauri\target\release\Foundry & Frontier Sync.exe
```
